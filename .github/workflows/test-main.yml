name: Strict Test Main Branch

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test-strict:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Setup Virtual Display (Xvfb)
      # Instalamos el monitor virtual para JavaFX
      run: sudo apt-get update && sudo apt-get install -y xvfb
        
    - name: Run All Tests (Strict and Basic)
      # Ejecutamos Maven usando xvfb-run
      run: xvfb-run mvn -B test --file ilerntale/pom.xml
